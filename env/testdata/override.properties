# -----------------------------------------------
# override.properties — local overrides
# -----------------------------------------------

app.env=local
app.debug=false
server.port=9090
database.host=127.0.0.1
database.name=lxdb_local
database.username=dev
database.password=devpassword
cache.host=127.0.0.1
logging.level=DEBUG
